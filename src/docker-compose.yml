version: "2"
services:
  evaluate:
    build: ./evaluate
    command: python /code/evaluate.py
    ports: 
      - "5000:5000"
    volumes:
      - ./evaluate:/code
      - ../data/models:/data
      - ../samples:/samples